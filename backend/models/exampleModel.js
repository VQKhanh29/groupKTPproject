// models/exampleModel.js
// Nếu dùng MongoDB + Mongoose, chuyển file này sang schema Mongoose.
// Đây là file placeholder để nhóm biết chỗ đặt model.
module.exports = {
  Example: {
    id: 'string',
    name: 'string',
    createdAt: Date.now(),
  },
};
